# Force LF line endings on shell scripts and Linux config files
# so they work when checked out on Linux/macOS hosts.
*.sh           text eol=lf
*.service      text eol=lf
*.conf         text eol=lf
deploy/**      text eol=lf

# Default
*              text=auto
